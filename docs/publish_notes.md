# Publish Notes

Target repository:

```text
https://github.com/ZnSO4-1/3D_modeling_and_application
```

The open-source package is prepared locally under:

```text
4/open_source/
```

Only source code, documentation, dependency metadata, a license, and small flowchart images are intended for publication. The following files are intentionally excluded:

- Raw LAS/LAZ point-cloud data.
- Generated ground-point LAS files.
- Generated DEM raster files.
- Generated PPTX report files.
- Python cache files.

Publishing from this environment is currently blocked because the connected GitHub token is expired. After reconnecting GitHub, the intended publish step is to create a feature branch and submit these files as a pull request to `ZnSO4-1/3D_modeling_and_application`.
